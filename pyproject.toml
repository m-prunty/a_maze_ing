[project]
name = "amazing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "Pillow>=10.0.0"
]

[tool.setuptools.packages.find]
where = ["src"]